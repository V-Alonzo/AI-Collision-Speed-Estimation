\documentclass[esp]{SpeedEstimationAI_paper}
\usepackage{authblk}
\usepackage{tabularx}
\usepackage{float}
\raggedbottom
%
% Publication Title
\title{Estimación de la Velocidad Vehicular Precolisión mediante Aprendizaje Profundo Multimodal Integrando Evidencia Visual Postcolisión y Datos Estructurados de Reconstrucción de Accidentes}

% Short title for the header (copy the main title if it is not too long)
\shorttitle{Reconstrucción de Velocidad Vehicular}
       
% Authors
\author[1]{Alberto Isaac Sánchez Tarango}
\author[1]{Alyson Melissa Sánchez Serratos}
\author[1]{Máximo Moises Zamudio Chávez}
\author[1]{Miguel Ángel Pérez Ávila}
\author[1]{Santiago Albarrán Organista}
\author[1]{Víctor Alonzo Estévez Chávez}

% Author Affiliations
\affil[1]{Tecnológico de Monterrey, Escuela de Ingeniería y Ciencias, Campus Toluca, Estado de México, México}

% Surname of the first author of the manuscript
\firstauthor{Sánchez, Sánchez, Zamudio}

%Contact Author Information
\contactauthor{Alberto Sánchez}           % Name and surname of the contact author
\email{A01771321@tec.mx}                                  % Contact Author Email

% Publication data (will be defined in the edition)
\thisvolume{XX}
\thisnumber{XX}
\thismonth{Month}
\thisyear{20XX}
\receptiondate{dd/mm/aaaa}
\acceptancedate{dd/mm/aaaa}
\publicationdate{dd/mm/aaaa}

% Place your particular definitions here
\newcommand{\vect}[1]{\mathbf{#1}}  % vectors

% Insert here the abstract in English language
\abstract{
Lorem ipsum dolor sit amet, consectetur adipiscing elit, sed do eiusmod tempor incididunt ut labore et dolore magna aliqua. Ut enim ad minim veniam, quis nostrud exercitation ullamco laboris nisi ut aliquip ex ea commodo consequat. Duis aute irure dolor in reprehenderit in voluptate velit esse cillum dolore eu fugiat nulla pariatur. Excepteur sint occaecat cupidatat non proident, sunt in culpa qui officia deserunt mollit anim id est laborum. Sed ut perspiciatis unde omnis iste natus error sit voluptatem accusantium doloremque laudantium, totam rem aperiam.}

% Insert here the keywords of your work in English language
\keywords{
Lorem ipsum, dolor sit amet, consectetur adipiscing, eiusmod tempor, labore et dolore}

% Insert here the abstract in Spanish language
\resumen{
La estimación de la velocidad vehicular precolisión constituye un problema de interés en la reconstrucción forense de accidentes debido a su relevancia para el análisis de la dinámica del impacto. En este trabajo se propone una arquitectura de aprendizaje profundo multimodal que integra evidencia visual postcolisión y datos estructurados de reconstrucción de accidentes provenientes del programa Crash Injury Research and Engineering Network (CIREN). La metodología combina representaciones visuales extraídas mediante redes neuronales convolucionales con variables técnicas del siniestro dentro de un esquema unificado de regresión. Los resultados experimentales muestran que el enfoque propuesto supera el desempeño de modelos unimodales basados exclusivamente en imágenes o datos tabulares, alcanzando una precisión aproximada de ±4.5 km/h. La mayor reducción del error se observó en el intervalo de 0–60 km/h, mientras que el desempeño en velocidades superiores se vio limitado por el desbalance inherente del conjunto de datos. Los hallazgos obtenidos evidencian el potencial de los enfoques multimodales para apoyar procesos automatizados de reconstrucción forense de accidentes vehiculares.}

% Insert here the keywords of your work in Spanish language
\palabrasclave{
velocidad vehicular precolisión, reconstrucción de accidentes, aprendizaje multimodal, daños estructurales vehiculares, redes neuronales convolucionales, transferencia de aprendizaje, datos estructurados.}

% Roman numerals for section numbering
\renewcommand{\thesection}{\Roman{section}}

% Start document
\begin{document}

% Include title, authors, abstract, etc.
\maketitle
\thispagestyle{fancy}
\printcontactdata

\section{INTRODUCCIÓN}

\firstword{L}{os} siniestros viales representan una problemática de alcance global con importantes implicaciones económicas, jurídicas y sociales. Según la Organización Mundial de la Salud, los accidentes de tránsito generan pérdidas equivalentes a aproximadamente el 3\% del producto interno bruto de la mayoría de los países, considerando costos asociados a atención médica, daños materiales, pérdida de productividad e impacto sobre los sistemas de aseguramiento \cite{WHO2023}. Además de sus consecuencias económicas directas, estos eventos suelen derivar en procesos periciales y judiciales en los que resulta necesario reconstruir de manera objetiva las circunstancias bajo las cuales ocurrió la colisión. En este contexto, la reconstrucción técnica de accidentes constituye una herramienta fundamental para determinar variables críticas como trayectorias de movimiento, mecanismos de impacto y velocidades de circulación, información utilizada como soporte técnico en investigaciones forenses, procedimientos legales y reclamaciones aseguradoras \cite{Brach2011,Fricke1990}.

Entre las variables involucradas en los procesos de reconstrucción, la velocidad vehicular previa al impacto posee especial relevancia debido a su relación directa con la energía liberada durante la colisión y, en consecuencia, con la magnitud de los daños estructurales y las lesiones sufridas por los ocupantes. Dado que la energía cinética es proporcional al cuadrado de la velocidad, incrementos relativamente moderados en esta variable producen aumentos sustanciales en la severidad del evento, afectando la dinámica del impacto, las deformaciones estructurales y las trayectorias posteriores de los vehículos involucrados \cite{Fricke1990}. Por esta razón, la estimación de la velocidad precolisión constituye una de las tareas centrales dentro de la reconstrucción forense de accidentes de tránsito \cite{Struble2014}.

Las metodologías tradicionalmente empleadas para esta tarea se fundamentan en principios de mecánica clásica y requieren información detallada sobre la deformación estructural de los vehículos involucrados. En muchos casos, estos procedimientos demandan inspecciones presenciales, mediciones especializadas y acceso directo a la evidencia física del siniestro \cite{Mrowicki2020,McHenry2001}. Sin embargo, en escenarios reales de investigación dicha evidencia frecuentemente resulta incompleta, ha sido alterada por intervenciones posteriores al accidente o simplemente ya no se encuentra disponible. Kerkhoff \cite{Kerkhoff1985} señala que, en numerosos casos, al momento de realizar una reconstrucción los únicos elementos disponibles corresponden al reporte policial y a las fotografías del accidente. De manera similar, la literatura especializada establece que las fotografías constituyen con frecuencia la única evidencia remanente de una colisión debido a la desaparición de rastros en la escena, la reparación o destrucción de los vehículos y la modificación posterior de sus componentes \cite{Linder2016}. Esta limitación motiva la búsqueda de metodologías capaces de extraer información cuantitativa a partir de evidencia observacional obtenida después del evento.

Paralelamente, los avances recientes en inteligencia artificial han demostrado una notable capacidad para aprender representaciones complejas a partir de información visual y estructurada. No obstante, la literatura especializada muestra que la mayoría de las investigaciones aplicadas al análisis automatizado de accidentes se han concentrado en tareas de clasificación, detección y segmentación de daños visibles. Una revisión sistemática reciente identificó más de 55 estudios en esta área, sin encontrar trabajos orientados específicamente a la estimación cuantitativa de variables dinámicas asociadas al estado previo al impacto \cite{Hasan2025}. Los pocos estudios que han explorado la estimación de velocidad precolisión presentan restricciones importantes en términos de tipo de colisión, rango de velocidades o disponibilidad de información \cite{Argote2022}. Asimismo, investigaciones recientes han caracterizado este problema como un desafío inverso de alta complejidad y han señalado explícitamente la existencia de una brecha de investigación en la aplicación de técnicas modernas de aprendizaje automático para la estimación de velocidad precolisión \cite{Zhao2021,Czajewski2026}.

Frente a este panorama, el presente trabajo investiga la viabilidad de estimar la velocidad vehicular precolisión mediante técnicas de aprendizaje profundo a partir de evidencia obtenida después del accidente. Para ello, se propone un enfoque multimodal que integra información visual proveniente de imágenes de daño vehicular con variables tabulares asociadas a la caracterización técnica del siniestro. A diferencia de los enfoques tradicionales de reconstrucción, que requieren inspección presencial y medición directa de perfiles de deformación, la estrategia propuesta explora la posibilidad de inferir esta variable dinámica a partir de evidencia observacional disponible después del evento.

La hipótesis subyacente es que la deformación estructural residual visible en las imágenes postcolisión, combinada con información contextual del siniestro, contiene información suficiente para aproximar las condiciones cinéticas previas al impacto sin necesidad de acceso físico directo al vehículo ni de instrumentación especializada de levantamiento pericial. Asimismo, se analiza de manera sistemática el aporte individual y conjunto de ambas modalidades de información con el objetivo de determinar su capacidad para predecir la velocidad vehicular precolisión.

Las principales contribuciones de este trabajo son: (i) la construcción de un conjunto de datos curado orientado a la estimación continua de velocidad precolisión, (ii) la sistematización de un proceso reproducible de preparación de datos forenses vehiculares con velocidades técnicamente verificadas, y (iii) la propuesta y evaluación de una arquitectura de aprendizaje profundo multimodal para la reconstrucción automatizada de velocidad a partir de evidencia postcolisión. Estas aportaciones buscan contribuir al desarrollo de metodologías periciales más objetivas, escalables y consistentes, fortaleciendo el uso de herramientas de inteligencia artificial como apoyo en la reconstrucción forense de accidentes viales.

\section{TRABAJO RELACIONADO}
La revisión de la literatura en torno a la estimación automatizada de variables dinámicas de colisión mediante técnicas de aprendizaje automático puede organizarse en cuatro líneas de investigación diferenciadas: (i) métodos de reconstrucción de velocidad precolisión basados en datos tabulares estructurados, (ii) métodos de reconstrucción basados en datos de deformación y el problema inverso, (iii) detección y clasificación de daño vehicular mediante aprendizaje profundo a partir de imágenes, y (iv) enfoques multimodales que integran fuentes heterogéneas de información para la predicción de variables relacionadas con accidentes. A continuación se examinan los trabajos más relevantes en cada una de estas líneas, identificando sus contribuciones, limitaciones y su relación con el problema abordado en el presente estudio.

\subsection{Estimación de velocidad precolisión}

Los primeros enfoques de aprendizaje automático para la estimación de velocidad precolisión se desarrollaron utilizando variables tabulares derivadas de pruebas de choque controladas. Cioroianu et al. \cite{Cioroianu2020} emplearon una red neuronal multicapa (MLP) para estimar la Velocidad Equivalente de Energía (EES) a partir de parámetros de deformación vehicular, obteniendo un error relativo medio cercano al 9\% y superando los métodos tradicionales basados en regresión. Posteriormente, Czajewski et al. \cite{Czajewski2026} evaluaron modelos de regresión lineal, MLP, Gradient Boosting y Random Forest sobre 399 pruebas de impacto frontal de la NHTSA, reportando el mejor desempeño mediante Random Forest e identificando explícitamente la estimación de velocidad precolisión mediante aprendizaje automático como una línea de investigación poco explorada.

Desde una perspectiva diferente, Zhao et al. \cite{Zhao2021} formularon la reconstrucción de condiciones de impacto como un problema inverso, empleando redes neuronales entrenadas con simulaciones de elementos finitos para recuperar velocidad y ángulo de colisión a partir de deformaciones residuales. Los autores demostraron que es posible identificar de forma aproximadamente única las condiciones iniciales del impacto a partir del estado final de deformación, aunque utilizando exclusivamente datos sintéticos.

\subsection{Análisis de daño vehicular mediante imágenes}

El uso de aprendizaje profundo para el análisis de daño vehicular ha experimentado un crecimiento significativo durante los últimos años. Patil et al. \cite{Patil2017} reportaron una exactitud del 89.5\% en clasificación de daños utilizando transferencia de aprendizaje. Kyu y Woraratpanya \cite{Kyu2020} alcanzaron exactitudes de 95.2\% y 94.6\% en detección de daño mediante VGG16 y VGG19, respectivamente, mientras que Jameel et al. \cite{Jameel2023} obtuvieron una precisión del 89.13\% y una exactitud global del 92\% utilizando una arquitectura Mask R-CNN. Asimismo, van Ruitenbeek y Bhulai \cite{vanRuitenbeek2022} demostraron que modelos entrenados sobre más de 10,000 imágenes pueden alcanzar desempeños comparables al de expertos humanos en evaluación de daños.

El antecedente más cercano al presente estudio corresponde a Argote et al. \cite{Argote2022}, quienes propusieron un modelo basado en imágenes para estimar Delta-V y clasificar la localización del impacto utilizando fotografías reales de accidentes. Los autores reportaron un MAE de 4.19 km/h y un MAPE de 16.2\% sobre 310 imágenes reales, además de una exactitud del 92\% en la clasificación de la localización del impacto. Sin embargo, el estudio se restringió a determinados tipos de colisión y no incorporó información tabular complementaria. Adicionalmente, la revisión sistemática de Hasan et al. \cite{Hasan2025}, que analizó más de 55 estudios relacionados con daño vehicular mediante inteligencia artificial, no identificó trabajos orientados específicamente a la estimación de velocidad precolisión, evidenciando una brecha de investigación en esta área.

\subsection{Enfoques multimodales}

La integración de múltiples modalidades de información ha emergido como una estrategia efectiva para mejorar el desempeño de modelos aplicados al análisis de accidentes. Bhowmik y Rateria \cite{Bhowmik2023} desarrollaron un modelo multimodal que combina imágenes y variables tabulares para detección automática de colisiones, obteniendo un ROC-AUC de 1.00 y superando los resultados de los enfoques unimodales. De manera similar, Liu et al. \cite{Liu2025} integraron información estructurada, características de infraestructura vial y descripciones textuales para la predicción de severidad de accidentes, alcanzando una exactitud de 86.5%, un recall de 86.8% y un F1-score de 0.712.

La relevancia de esta línea de investigación ha sido respaldada por la revisión sistemática y metaanálisis de Somvanshi et al. \cite{Somvanshi2026}, quienes analizaron 74 estudios y más de 2.1 millones de registros de accidentes, concluyendo que los modelos de aprendizaje profundo superan en promedio en 7.7 puntos porcentuales a los enfoques tradicionales y que las arquitecturas multimodales presentan ventajas consistentes respecto a los modelos unimodales.

En este contexto, el presente trabajo propone un enfoque multimodal orientado a la estimación de velocidad vehicular precolisión a partir de fotografías de daño estructural y variables tabulares asociadas al accidente. A diferencia de los trabajos previos, la propuesta combina ambas modalidades para abordar una tarea de regresión continua sobre accidentes reales de campo, contribuyendo a cerrar la brecha identificada en la literatura.


\section{METODOLOGÍA}

\subsection{Datos y preprocesamiento}
\subsubsection{Fuente de datos y criterios de construcción de la base de datos utilizada para el entrenamiento de los modelos predictivos de regresión}

La serie de componentes que han sido elaborados sobre la base de datos denominada \texttt{Crash Injury Research Engineering Network} (CIREN) y perteneciente al \texttt{NHTSA Crash Viewer}, se orientan a transformar información pública de siniestros vehiculares en una estructura analítica de importante utilidad para tareas de estimación de velocidad preimpacto a partir de evidencia postcolisión. Para ello, se parte de casos documentados con dos tipos de insumo complementario: metadatos del evento y del vehículo involucrado, junto con galerías fotográficas asociadas al daño observado tras la colisión. La lógica metodológica seguida no consiste únicamente en recolectar imágenes, sino en vincular cada fotografía válida con un contexto de siniestro suficientemente caracterizado en términos de severidad, dinámica de impacto y atributos generales del vehículo de tal manera que cada uno de estos elementos multimedia maximicen su valor cualitativo para la explicación del fenómeno físico de impacto y su relación directa con la velocidad a la que ocurrió.

El proceso se diseñó bajo tres principios: trazabilidad, reanudación y utilidad analítica. Primeramente, la trazabilidad garantiza que cada imagen final conserve su vínculo con el caso de origen y con el vehículo al que pertenece. Por otro lado, la reanudación permite continuar ejecuciones incompletas sin repetir el procesamiento ya realizado y, finalmente, la utilidad analítica asegura que el resultado no sea solo una galería visual, sino un conjunto estructurado de ejemplos listos para ser explorados y posteriormente consumidos por los modelos predictivos seleccionados.

\begin{figure}[H]
    \centering
    \includegraphics[width=\columnwidth]{Diagrams/Diagrama1.pdf}
    \caption{Diagrama de flujo general del pipeline para el tratamiento y persistencia de la base de datos de CIREN}
    \label{fig:diagrama1}
\end{figure}


\subsubsection{Caracterización del caso y selección del vehículo de interés}

Una vez identificado un caso particular desde la base de datos de \texttt{CIREN}, el flujo adquiere la responsabilidad de integrar la información descriptiva disponible para construir una representación compacta del evento. En esta etapa, se priorizan variables con valor directo para interpretación biomecánica y estructural del impacto tal y como lo son la severidad máxima de lesión humana tras el incidente, el cambio total de velocidad, la dirección del impacto, el patrón general de deformación, el plano principal de daño y características generales del vehículo, entre las cuales pueden ser encontradas: clase, masa base y carga reportada. Cada una de estas variables permiten contextualizar visualmente el daño y, al mismo tiempo, aportan atributos cuantitativos y categóricos relevantes para el diseño, preparación y entrenamiento de los modelos de inferencia.

Dado que un mismo caso puede contener múltiples vehículos o varias vistas de interés involucradas, se implementó una etapa de conciliación para identificar al vehículo principal que será estudiado. Esta decisión adquiere especial relevancia al permitir evitar la inadecuada mezcla de  imágenes pertenecientes a actores distintos dentro de un mismo siniestro, al igual que mejorar la coherencia entre la evidencia visual y los metadatos asociados. De esta forma, la unidad efectiva de análisis queda definida como el par compuesto por caso y vehículo principal, sobre el cual se organiza el resto del preprocesamiento.

\begin{figure}[H]
    \centering
    \includegraphics[width=\columnwidth]{Diagrams/Diagrama2.pdf}
    \caption{Diagrama de flujo que muestra la lógica de consolidación de un caso reportado en CIREN}
    \label{fig:diagrama1}
\end{figure}

\subsubsection{Catalogación de imágenes candidatas}

La colección fotográfica asociada a cada caso no se incorpora de manera directa a la base de datos final, sino que se lleva a cabo una fase de catalogación cuyo objetivo es reunir todas las vistas potencialmente útiles del vehículo seleccionado y registrar su procedencia. En esta etapa, cada imagen candidata conserva información descriptiva suficiente para identificarla posteriormente, incluyendo su relación con el vehículo, la vista o categoría visual de la que proviene y un identificador estable y estrictamente incremental para evitar la sobrescritura de archivos en tiempo de ejecución.

Metodológicamente, esta fase cumple dos funciones, la primera es separar el descubrimiento de contenido de la validación visual, lo cual reduce reprocesamiento en ejecuciones sucesivas y la segunda es imponer una preselección semántica: se descartan categorías de imágenes que típicamente aportan poco a la reconstrucción del daño estructural como lo pueden ser: tomas irrelevantes que no pertenezcan directamente a los vehículos involucrados, vistas no exteriores del automóvil o material complementario de baja utilidad para análisis forense del vehículo. Consecuentemente, el conjunto candidato se concentra en fotografías con mayor probabilidad de contener evidencia visual de interés.

\begin{figure}[H]
    \centering
    \includegraphics[width=\columnwidth]{Diagrams/Diagrama3.pdf}
    \caption{Diagrama de estados que muestra la transición de una imagen candidata a persistida o descartada}
    \label{fig:diagrama1}
\end{figure}

\subsubsection{Filtrado visual y validación de evidencia dañada}

Después de la catalogación, cada candidato se somete a una etapa de validación visual artificial orientada a confirmar que la fotografía contiene evidencia utilizable de daño vehicular. La lógica de esta etapa se centra en evitar que la base de datos final contenga imágenes sin daño apreciable o con contenido visual poco representativo del fenómeno de interés. Dicho de otro modo, no toda imagen asociada a un caso CIREN es útil para modelar severidad de colisión o estimar velocidad preimpacto. Por ello, fue necesario incorporar un filtro automatizado que priorice señales visibles de deformación estructural para reducir la probabilidad de ocurrencia de esta serie de casos innecesarios.

\begin{figure}[H]
    \centering
    \includegraphics[width=\columnwidth]{Diagrams/Diagrama4.pdf}
    \caption{Diagrama de flujo donde se muestra el proceso de validación visual de una imagen extraída de un caso en CIREN}
    \label{fig:diagrama1}
\end{figure}


\subsubsection{Normalización de salidas para análisis posterior}

El resultado del preprocesamiento de imágenes y características extraídas desde CIREN es organizado en dos niveles complementarios: caso e imagen. El primero se concentra en la caracterización del siniestro y del vehículo, mientras que el segundo tiene el objetivo de conservar la evidencia visual validada con su referencia al caso correspondiente. Esta separación permite analizar la información desde una perspectiva estadística o computacional sin perder la granularidad particular de cada fotografía.

\begin{figure}[H]
    \centering
    \includegraphics[width=\columnwidth]{Diagrams/Diagrama7.pdf}
    \caption{Diagrama de entidad relación del conjunto resultante del preprocesamiento y extracción de datos de CIREN}
    \label{fig:diagrama1}
\end{figure}

\subsection{Diseño Experimental}

Con el propósito de analizar la viabilidad de estimar la velocidad vehicular precolisión a partir de información disponible después de un siniestro, se definió un diseño experimental compuesto por tres enfoques complementarios basados en distintas modalidades de datos. Esta estrategia permitió evaluar de manera independiente la contribución de la evidencia visual, la información estructurada y la combinación de ambas fuentes dentro de un mismo marco de predicción.

\subsubsection{Enfoque basado en imágenes}

La primera hipótesis plantea que las deformaciones estructurales observables en imágenes postcolisión contienen información suficiente para inferir la velocidad vehicular precolisión. Bajo este supuesto, se desarrolló un modelo de aprendizaje profundo basado exclusivamente en imágenes, orientado a identificar patrones visuales asociados con distintos niveles de severidad de impacto y utilizarlos para realizar una estimación continua de velocidad.

\subsubsection{Enfoque basado en variables tabulares}

La segunda hipótesis establece que las variables técnicas registradas durante los procesos de reconstrucción de accidentes contienen información predictiva suficiente para estimar la velocidad precolisión sin necesidad de evidencia visual. Para evaluar esta hipótesis se desarrolló un modelo basado exclusivamente en variables tabulares provenientes de la base de datos CIREN, incluyendo características relacionadas con el vehículo, la dinámica del impacto y la descripción estructural del daño.

\subsubsection{Enfoque multimodal}

La tercera hipótesis propone que la integración de evidencia visual e información estructurada permite construir una representación más completa del accidente que cualquiera de las modalidades por separado. Con base en este planteamiento, se diseñó una arquitectura multimodal capaz de combinar simultáneamente características extraídas de imágenes postcolisión y variables tabulares asociadas al siniestro, con el objetivo de determinar si la fusión de ambas fuentes de información mejora la precisión de las estimaciones de velocidad.

La evaluación conjunta de estos tres enfoques permite cuantificar el aporte individual de cada modalidad y analizar el beneficio potencial derivado de su integración dentro de un esquema multimodal de aprendizaje profundo orientado a la reconstrucción forense de accidentes.

\subsection{Modelo Basado en Imágenes}

Con el objetivo de evaluar la capacidad predictiva de la evidencia visual disponible después de una colisión vehicular, se formuló la hipótesis de que la velocidad precolisión puede estimarse a partir de las deformaciones estructurales observables en imágenes postimpacto, prescindiendo completamente de variables tabulares o mediciones físicas directas del vehículo. Esta hipótesis se fundamenta en el hecho de que la magnitud y distribución del daño estructural constituyen una manifestación física de la energía disipada durante la colisión y, por tanto, contienen información indirectamente relacionada con la velocidad del impacto.

Para validar este planteamiento se desarrolló una arquitectura de visión computacional denominada \textit{ImageVelocityEstimator}, diseñada para resolver el problema como una tarea de regresión supervisada. En esta formulación, la entrada del modelo corresponde a una imagen fotográfica de un vehículo involucrado en un accidente, mientras que la salida consiste en una estimación continua de la velocidad precolisión expresada en kilómetros por hora.

\subsubsection{Arquitectura Propuesta}

La arquitectura desarrollada se construyó siguiendo un enfoque de \textit{Transfer Learning}, aprovechando conocimiento previamente adquirido por modelos entrenados sobre grandes volúmenes de datos visuales. Para ello se seleccionó la arquitectura ResNet50 como extractor principal de características, utilizando pesos preentrenados sobre el conjunto de datos ImageNet.

La elección de ResNet50 se fundamentó en tres factores principales. En primer lugar, esta arquitectura ha demostrado un desempeño sobresaliente en tareas de clasificación visual y extracción de características de alto nivel. En segundo lugar, la utilización de conexiones residuales permite entrenar redes profundas mitigando problemas asociados a la desaparición del gradiente. Finalmente, existe amplia evidencia en la literatura que respalda su capacidad para adaptarse exitosamente a dominios especializados mediante procesos de ajuste fino (\textit{Fine-Tuning}).

Dentro de la arquitectura propuesta, ResNet50 actúa como un extractor jerárquico de representaciones visuales. Las primeras capas de la red capturan patrones elementales como bordes, texturas y contornos, mientras que las capas más profundas aprenden estructuras visuales progresivamente más complejas relacionadas con deformaciones de carrocería, patrones de aplastamiento, regiones de impacto y otros indicadores visuales asociados con la severidad de la colisión.

Con el propósito de adaptar dichas representaciones al problema específico de estimación de velocidad vehicular, se implementó una etapa de \textit{Fine-Tuning} controlado sobre las capas más profundas de ResNet50. Esta estrategia permitió especializar las características visuales previamente aprendidas para capturar patrones particulares presentes en vehículos siniestrados, conservando simultáneamente la capacidad general de extracción de características adquirida durante el entrenamiento original sobre ImageNet.

Posteriormente, las características visuales generadas por ResNet50 son procesadas mediante un bloque de generación de embeddings encargado de proyectar las representaciones originales de alta dimensionalidad hacia un espacio latente compacto de 512 dimensiones. Este espacio latente fue diseñado con el objetivo de concentrar la información visual más relevante para la tarea de regresión, reduciendo redundancias y favoreciendo la construcción de representaciones semánticamente organizadas del daño vehicular.

Finalmente, los embeddings generados son utilizados como entrada de una red neuronal completamente conectada (\textit{Fully Connected Regressor}), responsable de realizar la estimación final de velocidad mediante una única neurona de salida con activación lineal. La Figura~\ref{fig:ImageVelocityEstimator_Arquitectura} muestra la arquitectura general implementada para el modelo ImageVelocityEstimator.

\begin{figure}[H] \centering \includegraphics[width=\columnwidth]{Diagrams/ImageVelocityEstimator-arch.pdf} \caption{Arquitectura de ImageVelocityEstimator} \label{fig:ImageVelocityEstimator_Arquitectura} \end{figure}


\subsubsection{Proceso de Entrenamiento}

Con el objetivo de identificar la configuración de mayor desempeño, se llevaron a cabo múltiples iteraciones experimentales evaluando distintas variantes arquitectónicas y estrategias de optimización.

Las configuraciones exploradas incluyeron diferentes esquemas de ajuste fino de ResNet50, funciones de optimización, tasas de aprendizaje, estrategias de regularización, técnicas de aumento de datos (\textit{Data Augmentation}), estructuras alternativas para el espacio latente y diversas configuraciones para la red de regresión final.

Las técnicas de aumento de datos fueron incorporadas con el propósito de incrementar la variabilidad visual observada durante el entrenamiento y mejorar la capacidad de generalización del modelo frente a cambios en iluminación, orientación, encuadre y condiciones de captura de las imágenes. Paralelamente, se evaluaron diferentes mecanismos de regularización para reducir el riesgo de sobreajuste derivado del tamaño limitado del conjunto de datos disponible.

La selección de la configuración final se realizó mediante evaluación sistemática sobre los conjuntos de entrenamiento y validación, considerando principalmente métricas de error absoluto medio (MAE) y la estabilidad observada durante el proceso de convergencia.

\subsubsection{Configuración Experimental Seleccionada}

Como resultado del proceso iterativo de experimentación descrito anteriormente, se identificó una configuración que obtuvo el mejor desempeño global entre las distintas variantes evaluadas para ImageVelocityEstimator. Esta configuración fue posteriormente adoptada como la versión definitiva del modelo visual y utilizada como encoder dentro de la arquitectura multimodal HybridVelocityEstimator.

El experimento seleccionado se construyó sobre una arquitectura ResNet50 preentrenada sobre ImageNet utilizando una estrategia de \textit{Transfer Learning} con ajuste fino parcial (\textit{Fine-Tuning}) de las capas más profundas del backbone. Específicamente, se habilitó el entrenamiento de los bloques correspondientes a \texttt{resnet.layer4}, permitiendo especializar las representaciones visuales de alto nivel para la identificación de patrones asociados con daños vehiculares y severidad de impacto, mientras que las capas anteriores conservaron los pesos adquiridos durante el preentrenamiento.

Con el propósito de incrementar la capacidad de representación de la arquitectura, se incorporó un bloque personalizado de generación de embeddings compuesto por una capa completamente conectada que proyecta las características extraídas por ResNet50 desde 2048 dimensiones hacia un espacio latente compacto de 512 dimensiones. Este bloque incluye Batch Normalization, función de activación GELU y una capa Dropout con probabilidad de 0.3, favoreciendo la estabilidad del entrenamiento y reduciendo el riesgo de sobreajuste.

La etapa final de regresión fue implementada mediante una arquitectura \textit{Fully Connected} compuesta por una capa oculta de 128 neuronas seguida de Batch Normalization, activación GELU y Dropout con probabilidad de 0.2. Finalmente, una neurona lineal de salida genera la estimación continua de velocidad precolisión. Esta configuración fue seleccionada tras evaluar distintas alternativas para la dimensionalidad de los embeddings y la complejidad del regresor, observándose un equilibrio favorable entre capacidad de representación y generalización.

Durante el entrenamiento se utilizó un conjunto de datos aumentado compuesto por 15,513 imágenes, generado mediante técnicas de \textit{Data Augmentation} orientadas a incrementar la variabilidad visual observada por la red neuronal. Todas las imágenes fueron normalizadas utilizando estadísticas calculadas sobre el conjunto de entrenamiento y posteriormente redimensionadas a una resolución de $224 \times 224$ píxeles, compatible con la arquitectura ResNet50.

La optimización se realizó durante un máximo de 60 épocas utilizando un tamaño de lote de 128 muestras y una tasa de aprendizaje inicial de $1\times10^{-4}$. Adicionalmente, se incorporó un mecanismo de \textit{Early Stopping} con una paciencia de 20 épocas para prevenir sobreajuste y detener automáticamente el entrenamiento cuando no se observaran mejoras significativas en el desempeño de validación.

Una de las modificaciones más relevantes respecto a experimentos anteriores consistió en sustituir la función de pérdida \textit{Smooth L1 Loss} por \textit{L1 Loss} (Error Absoluto Medio). Esta decisión permitió alinear directamente el proceso de optimización con la métrica principal utilizada durante la evaluación del modelo, favoreciendo una reducción más consistente del error absoluto medio observado sobre los conjuntos de validación y prueba.

La Tabla~\ref{tab:imagevelocity_best_config} resume los principales hiperparámetros y características de la configuración seleccionada.

\begin{table}[H]
	\caption{Configuración experimental seleccionada para ImageVelocityEstimator}
	\label{tab:imagevelocity_best_config}
	\centering
	\footnotesize
	\setlength{\tabcolsep}{2pt}
	\renewcommand{\arraystretch}{1.05}
	
	\begin{tabularx}{\columnwidth}{|p{3.2cm}|X|}
		\hline
		\textbf{Parámetro} & \textbf{Valor} \\
		\hline
		
		Backbone &
		ResNet50 preentrenada en ImageNet \\
		\hline
		
		Fine-Tuning &
		Descongelamiento de \texttt{layer4} \\
		\hline
		
		Resolución de entrada &
		$224 \times 224$ píxeles \\
		\hline
		
		Dataset de entrenamiento &
		15\,513 imágenes aumentadas mediante Data Augmentation \\
		\hline
		
		Embedding visual &
		512 dimensiones \\
		\hline
		
		Función de activación &
		GELU \\
		\hline
		
		Dropout (Embedding Layer) &
		0.3 \\
		\hline
		
		Dropout (Regressor) &
		0.2 \\
		\hline
		
		Batch Size &
		128 \\
		\hline
		
		Learning Rate &
		$1\times10^{-4}$ \\
		\hline
		
		Épocas máximas &
		60 \\
		\hline
		
		Early Stopping &
		Paciencia de 20 épocas \\
		\hline
		
		Función de pérdida &
		L1 Loss (MAE) \\
		\hline
		
		Optimizador &
		AdamW \\
		\hline
		
		Precisión de entrenamiento &
		bf16-mixed \\
		\hline
		
		Semilla aleatoria &
		42 \\
		\hline
		
	\end{tabularx}
\end{table}


Todos los experimentos correspondientes a ImageVelocityEstimator fueron ejecutados sobre una estación de trabajo portátil equipada con un procesador Intel Core i9-14900HX de 24 núcleos (32 hilos de ejecución), una unidad de procesamiento gráfico NVIDIA GeForce RTX 4060 Laptop GPU y 16 GB de memoria RAM. El entorno de ejecución estuvo basado en Debian GNU/Linux 12 (Bookworm), utilizando aceleración por GPU mediante CUDA y entrenamiento en precisión mixta (\textit{bf16-mixed}). Esta configuración permitió completar el proceso de entrenamiento y evaluación del modelo seleccionado en aproximadamente 1281.4 segundos (21.36 minutos), proporcionando recursos computacionales suficientes para el entrenamiento eficiente de las arquitecturas multimodales propuestas.

\subsubsection{Análisis del Espacio Latente}

Además de la evaluación tradicional basada en métricas de regresión, se realizó un análisis exploratorio orientado a comprender la calidad de las representaciones internas aprendidas por la red neuronal.

Para ello se extrajeron los embeddings visuales de 512 dimensiones generados inmediatamente antes de la etapa de regresión. Desde una perspectiva conceptual, estos embeddings constituyen la representación interna que el modelo construye sobre cada vehículo colisionado, codificando en forma matemática las características visuales que considera relevantes para inferir la velocidad del impacto.

Con el objetivo de analizar la organización de este espacio latente, se aplicaron múltiples técnicas de reducción de dimensionalidad que permitieron proyectar los embeddings hacia espacios bidimensionales para su posterior visualización. Este procedimiento tuvo como finalidad determinar si las representaciones aprendidas por la red exhibían patrones de agrupamiento asociados a diferentes niveles de velocidad.


\subsection{Modelo Basado en Variables Tabulares}

Con el objetivo de evaluar la capacidad predictiva de la información estructurada disponible en procesos de reconstrucción de accidentes de tránsito, se formuló la hipótesis de que la velocidad vehicular precolisión puede estimarse a partir de variables tabulares derivadas exclusivamente de investigaciones forenses, prescindiendo del análisis de imágenes postcolisión. Para validar esta hipótesis se diseñó un pipeline de preprocesamiento orientado a transformar las variables originales en un conjunto compacto de características predictivas, sobre el cual se entrenó una Red Neuronal Multicapa para estimar la velocidad asociada al impacto.

\subsubsection{Pipeline de Preprocesamiento}

Como etapa inicial, se eliminaron todos aquellos registros que no contenían una medición válida de velocidad asociada al impacto, dado que la ausencia de la variable objetivo imposibilita el entrenamiento supervisado. Asimismo, se excluyeron aquellas columnas consideradas no informativas para el modelado.

Con el propósito de incrementar el número de observaciones disponibles sin alterar la naturaleza física de la variable objetivo, se integraron dos fuentes de información cinemática presentes en la base de datos CIREN: la medición reconstruida del delta-V total (\texttt{totalDeltaV}) y la estimación de velocidad equivalente a barrera obtenida mediante simulación computacional (\texttt{dvBarrierEquivalentSpeedDescription}). Cuando ambas mediciones estaban disponibles para un mismo registro, se priorizó el valor reconstruido por su mayor fidelidad a las condiciones reales del accidente. En ausencia de esta medición, se utilizó la estimación derivada de simulación como aproximación cinemática. Esta estrategia permitió ampliar la cobertura del conjunto de datos y obtener un total de 288 registros únicos válidos para el entrenamiento y evaluación del modelo.

Las variables numéricas continuas fueron procesadas mediante imputación por mediana y posterior estandarización Z-score. La imputación por mediana se seleccionó debido a su robustez frente a valores atípicos, mientras que la normalización permitió garantizar escalas homogéneas entre variables y mejorar la estabilidad numérica durante el entrenamiento de la red neuronal.

Las variables categóricas nominales fueron imputadas inicialmente mediante una categoría genérica para gestionar valores faltantes y posteriormente transformadas mediante \textit{Target Encoding}. Bajo este esquema, cada categoría fue reemplazada por un valor numérico correspondiente a la media condicional de la variable objetivo asociada a dicha categoría, calculada exclusivamente sobre el conjunto de entrenamiento para evitar fuga de información hacia los conjuntos de validación y prueba. Esta estrategia permitió representar información categórica preservando su relación estadística con la variable objetivo sin incrementar la dimensionalidad del espacio de entrada.

Dado que ciertas variables presentaban naturaleza angular y, por tanto, una estructura circular que no puede representarse adecuadamente mediante codificaciones lineales convencionales, se aplicó una transformación trigonométrica basada en funciones seno y coseno. Cada ángulo $\theta$ fue transformado en dos componentes ortogonales, $\sin(\theta)$ y $\cos(\theta)$, permitiendo preservar la continuidad del espacio angular y evitando discontinuidades artificiales entre valores equivalentes próximos a los límites del intervalo.

Las variables ordinales fueron codificadas preservando explícitamente su jerarquía natural. La variable \texttt{severityDescription} fue transformada mediante codificación ordinal respetando el orden de gravedad establecido en la escala original. De forma similar, la variable \texttt{mais} (\textit{Maximum Abbreviated Injury Scale}) requirió una etapa previa de extracción de su componente numérico antes de su codificación ordinal, debido a que su representación original contenía elementos de formato no estrictamente numéricos.

Posteriormente, se aplicó un procedimiento de selección de características orientado a eliminar variables redundantes o irrelevantes y reducir el ruido presente en el espacio de entrada. Este proceso se desarrolló en tres etapas sucesivas.

En primer lugar, se eliminaron las variables constantes o cuasi‑constantes, definidas como aquellas cuyo número de valores únicos se encontraba por debajo de un umbral mínimo ($\leq 1$), dado que estas características carecen de capacidad discriminatoria respecto a la variable objetivo.

En segundo lugar, se calculó la Información Mutua (\textit{Mutual Information}, MI) entre cada predictor y la velocidad objetivo. Esta métrica cuantifica la dependencia estadística entre dos variables sin asumir relaciones lineales. Aquellas variables cuyo valor de MI fue inferior a $0.001$ fueron descartadas por aportar una cantidad despreciable de información relevante para la predicción.

Finalmente, se realizó un análisis de correlación de Pearson entre las variables restantes. Cuando dos predictores presentaban una correlación superior a $0.90$, indicativa de multicolinealidad severa, se conservó únicamente aquella variable con mayor valor de Información Mutua respecto a la variable objetivo, eliminando así características redundantes.

Como resultado del proceso de preprocesamiento y selección de características, se obtuvo un conjunto final compuesto por siete variables predictoras: el peso en vacío del vehículo (\texttt{curbWeightKg}), la clase vehicular (\texttt{vehicleClass}), el plano principal de daño (\texttt{damagePlaneDescription}), las componentes seno y coseno de la dirección de la fuerza de impacto (\texttt{forceDirection\_sin} y \texttt{forceDirection\_cos}), la descripción de severidad del accidente (\texttt{severityDescription}) y el índice máximo de lesión abreviada (\texttt{mais}). Estas variables constituyeron el conjunto de entrada utilizado para el entrenamiento del modelo de predicción.

La Tabla~\ref{tab:tabFeaturesTabular} resume las variables seleccionadas, su tipo original y las transformaciones aplicadas durante el proceso de preprocesamiento.

\begin{table}[H]
\caption{Variables utilizadas por el modelo tabular (288 registros, 7 características)}
\label{tab:tabFeaturesTabular}
\centering
\footnotesize
\setlength{\tabcolsep}{2pt}
\renewcommand{\arraystretch}{1.05}

\begin{tabular}{|p{2.4cm}|p{2.4cm}|p{2.6cm}|}
\hline
\textbf{Variable} &
\textbf{Tipo Original} &
\textbf{Transformación} \\
\hline

curbWeightKg &
Numérica continua &
Imputación mediana + Z-score \\
\hline

vehicleClass &
Categórica nominal &
Imputación categórica + Target Encoding \\
\hline

damagePlaneDes &
Categórica nominal &
Imputación categórica + Target Encoding \\
\hline

forceDirection\_sin &
Cíclica &
sin($\theta$) \\
\hline

forceDirection\_cos &
Cíclica &
cos($\theta$) \\
\hline

severityDescription &
Ordinal &
Codificación ordinal \\
\hline

mais &
Ordinal &
Extracción numérica + codificación ordinal \\
\hline

\end{tabular}
\end{table}

\subsubsection{Modelo de Regresión Basado en Red Neuronal Multicapa}

El modelo fue desarrollado y ejecutado en una estación de trabajo equipada con un procesador Intel® Core™ i9-14900HX (2.20 GHz), 32 GB de memoria RAM y una unidad de procesamiento gráfico NVIDIA GeForce RTX 4070 con 8 GB de memoria VRAM. El sistema operativo utilizado fue de 64 bits con arquitectura x64.

Para la estimación de la velocidad vehicular precolisión se implementó una Red Neuronal Multicapa (\textit{Multilayer Perceptron}, MLP) completamente conectada. La arquitectura estuvo compuesta por tres capas ocultas de 64, 32 y 16 neuronas, respectivamente, siguiendo una estructura de tipo embudo que reduce progresivamente la dimensionalidad de las representaciones internas aprendidas por la red.

Todas las capas ocultas utilizaron la función de activación ReLU (\textit{Rectified Linear Unit}), mientras que la capa de salida consistió en una única neurona lineal acorde con la naturaleza continua de la variable objetivo. Con el fin de reducir el riesgo de sobreajuste, se incorporó regularización mediante Dropout con una tasa del 5\%, aplicada después de cada capa oculta.

El conjunto de datos fue dividido en tres subconjuntos mutuamente excluyentes: 64\% para entrenamiento, 16\% para validación y 20\% para prueba. El entrenamiento se realizó utilizando un tamaño de lote de 16 muestras y una tasa de aprendizaje de 0.0003. La función objetivo optimizada durante el entrenamiento fue el Error Cuadrático Medio (MSE), mientras que el desempeño predictivo se evaluó mediante el Error Absoluto Medio (MAE), expresado en kilómetros por hora.

Se estableció un máximo de 100 épocas de entrenamiento y se aplicó una estrategia de parada anticipada (\textit{Early Stopping}) con una paciencia de 10 épocas consecutivas sin mejora en la pérdida de validación. Este mecanismo permitió detener automáticamente el entrenamiento cuando el modelo dejaba de mostrar mejoras significativas, reduciendo el riesgo de sobreajuste y optimizando el tiempo de cómputo.

La Tabla~\ref{tab:tabMLP} presenta la arquitectura completa de la red neuronal y los hiperparámetros utilizados durante el entrenamiento del modelo.

\begin{table}[H]
\caption{Arquitectura e hiperparámetros de la Red Neuronal Multicapa}
\label{tab:tabMLP}
\centering
\footnotesize
\setlength{\tabcolsep}{2pt}
\renewcommand{\arraystretch}{1.05}

\begin{tabularx}{\columnwidth}{|p{3.2cm}|p{2.2cm}|X|}
\hline
\textbf{Parámetro} &
\textbf{Valor} &
\textbf{Descripción} \\
\hline

Tipo de arquitectura &
Fully connected (MLP) &
Red neuronal completamente conectada \\
\hline

Número de capas ocultas &
3 &
Estructura tipo embudo \\
\hline

Neuronas — Capa 1 &
64 &
Primera capa oculta \\
\hline

Neuronas — Capa 2 &
32 &
Segunda capa oculta \\
\hline

Neuronas — Capa 3 &
16 &
Tercera capa oculta \\
\hline

Función de activación &
ReLU &
Rectified Linear Unit en capas ocultas \\
\hline

Capa de salida &
1 neurona lineal &
Regresión de variable continua \\
\hline

Regularización &
Dropout 5\% &
Aplicado tras cada capa oculta \\
\hline

Tamaño de batch &
16 &
Muestras por iteración de gradiente \\
\hline

Tasa de aprendizaje &
0.0003 &
Learning rate del optimizador \\
\hline

Épocas máximas &
100 &
Límite superior de iteraciones de entrenamiento \\
\hline

Early Stopping &
10 épocas &
Paciencia sin mejora en pérdida de validación \\
\hline

Función de pérdida &
MSE &
Error Cuadrático Medio \\
\hline

Métrica de evaluación &
MAE &
Error Absoluto Medio (km/h) \\
\hline

\end{tabularx}
\end{table}

\subsection{Modelo Híbrido}

Los resultados obtenidos durante el desarrollo de los enfoques unimodales permitieron identificar fortalezas y limitaciones complementarias en cada modalidad de información. Por una parte, el modelo basado en imágenes demostró la capacidad de aprender representaciones visuales asociadas con la severidad de los daños estructurales y con la velocidad precolisión, aunque su desempeño se encontraba limitado por la ausencia de información contextual y técnica del accidente. Por otra parte, el modelo basado en variables tabulares logró capturar relaciones estadísticas relevantes entre atributos derivados de procesos de reconstrucción forense y la velocidad de impacto, pero carecía de acceso a la información visual contenida en las deformaciones observables de los vehículos involucrados.

A partir de estas observaciones se formuló una nueva hipótesis de investigación: si ambas modalidades describen aspectos complementarios del mismo fenómeno físico, entonces la integración conjunta de información visual e información estructurada podría permitir la construcción de representaciones más completas del siniestro y, en consecuencia, mejorar la precisión de las estimaciones de velocidad precolisión.

Con el propósito de validar esta hipótesis se desarrolló una arquitectura multimodal denominada \textit{HybridVelocityEstimator}, diseñada para combinar simultáneamente evidencia visual proveniente de imágenes postcolisión y variables tabulares derivadas de procesos de reconstrucción de accidentes. El objetivo principal de esta arquitectura consiste en aprovechar las fortalezas de cada modalidad dentro de un único proceso de inferencia, permitiendo que el modelo disponga de una descripción más rica y completa de cada evento.

Desde una perspectiva conceptual, el modelo busca aproximar una función donde la velocidad precolisión depende tanto de las características visuales observables en el vehículo siniestrado como de los atributos técnicos asociados al caso. En lugar de obligar a una única fuente de información a explicar completamente el fenómeno, la arquitectura distribuye el aprendizaje entre dos ramas especializadas que posteriormente son fusionadas dentro de una representación conjunta.

\subsubsection{Arquitectura Propuesta}

La arquitectura recibe dos entradas para cada muestra del conjunto de datos:

\begin{itemize}
	\item Una imagen correspondiente al vehículo colisionado.
	\item Un vector de variables tabulares asociado al mismo caso.
\end{itemize}

La rama visual reutiliza el modelo \textit{ImageVelocityEstimator} previamente desarrollado, empleándolo como un encoder especializado de características visuales. Bajo esta configuración, la imagen es procesada por el backbone ResNet50 y por el bloque de generación de embeddings hasta producir una representación latente de 512 dimensiones.

Este embedding constituye la representación interna que el modelo construye sobre la evidencia visual disponible y concentra información relacionada con la severidad del daño estructural, los patrones de deformación observados en la carrocería, la distribución espacial de los impactos y otras características visuales relevantes para la estimación de velocidad.

De manera paralela, las variables tabulares correspondientes al mismo caso son procesadas mediante los pipelines de preprocesamiento descritos en la sección anterior, obteniendo una representación estructurada de las características técnicas del accidente.

Una vez obtenidas ambas representaciones, se aplica una operación de concatenación que constituye el mecanismo de fusión multimodal empleado por la arquitectura. Esta estrategia permite preservar íntegramente la información aportada por cada modalidad antes de iniciar el proceso de aprendizaje conjunto.

Como resultado de esta etapa se genera un vector multimodal compuesto por:

\begin{center}
	Embedding Visual (512 dimensiones)
	+
	Variables Tabulares
\end{center}

La Figura~\ref{fig:HybridVelocityEstimator_Arquitectura} muestra la arquitectura general implementada para el modelo HybridVelocityEstimator.

\begin{figure}[H]
	\centering
	\includegraphics[width=\columnwidth]{Diagrams/HybridVelocityEstimator-arch.pdf}
	\caption{Arquitectura propuesta para HybridVelocityEstimator}
	\label{fig:HybridVelocityEstimator_Arquitectura}
\end{figure}

\subsubsection{Regresor Multimodal}

La representación fusionada es procesada mediante una red neuronal completamente conectada diseñada específicamente para aprender relaciones cruzadas entre ambas modalidades.

La arquitectura implementada utiliza una estructura de reducción progresiva de dimensionalidad compuesta por capas ocultas de 512, 256, 128 y 64 neuronas respectivamente. Cada bloque incorpora Batch Normalization, función de activación GELU y mecanismos de regularización mediante Dropout.

La utilización de Batch Normalization contribuye a estabilizar la distribución interna de activaciones durante el entrenamiento, facilitando la convergencia del modelo y reduciendo la sensibilidad a la inicialización de parámetros. Por su parte, la activación GELU introduce una transformación no lineal suave que ha demostrado un desempeño superior al de funciones clásicas como ReLU en diversas arquitecturas profundas. Finalmente, la incorporación de Dropout permite reducir el riesgo de sobreajuste mediante la desactivación aleatoria de neuronas durante el entrenamiento.

La estructura de reducción progresiva fue diseñada con el objetivo de permitir que el modelo aprenda representaciones cada vez más abstractas a medida que la información avanza a través de la red. Este proceso favorece la construcción de relaciones complejas entre características visuales y variables tabulares, capturando interacciones que difícilmente podrían ser modeladas por cualquiera de las modalidades de forma independiente.

La arquitectura concluye con una única neurona lineal de salida encargada de generar la estimación continua de velocidad precolisión.

\subsubsection{Proceso de Entrenamiento}

Una vez definida la arquitectura, se llevó a cabo una etapa extensa de experimentación orientada a optimizar el desempeño del modelo. Al igual que en los enfoques anteriores, se evaluaron múltiples configuraciones relacionadas con hiperparámetros, tasas de aprendizaje, mecanismos de regularización y configuraciones internas de la red de regresión.

La evaluación sistemática de estas configuraciones permitió identificar la combinación de parámetros con mejor desempeño sobre los conjuntos de validación y prueba. Durante esta etapa se observó un comportamiento particularmente estable de las curvas de entrenamiento, caracterizado por una convergencia consistente y por la ausencia de divergencias significativas entre las métricas de entrenamiento y validación.

Este comportamiento constituye un indicio de que la arquitectura fue capaz de aprovechar la información adicional proporcionada por ambas modalidades para construir representaciones más robustas y generalizables que las obtenidas por los enfoques unimodales.

\subsubsection{Configuración Experimental Seleccionada}

Como resultado del proceso iterativo de experimentación realizado sobre la arquitectura multimodal propuesta, se identificó una configuración que obtuvo el mejor desempeño global entre todas las variantes evaluadas para HybridVelocityEstimator. Esta configuración fue seleccionada como modelo final debido a su capacidad para integrar eficazmente la información visual proveniente del encoder ImageVelocityEstimator con las variables tabulares derivadas del proceso de reconstrucción forense.

La arquitectura fue entrenada utilizando un conjunto de datos multimodal compuesto por 5\,202 muestras, cada una formada por una imagen postcolisión y un conjunto de variables estructuradas previamente seleccionadas mediante los procesos de preprocesamiento y análisis descritos en secciones anteriores. El conjunto de datos fue particionado en subconjuntos de entrenamiento, validación y prueba siguiendo una estrategia estratificada, obteniéndose 3\,329 muestras para entrenamiento, 832 para validación y 1\,041 para evaluación final.

Para la modalidad visual se reutilizó el encoder previamente entrenado mediante ImageVelocityEstimator, empleando los pesos correspondientes al experimento visual de mejor desempeño. Este encoder genera una representación latente de 512 dimensiones que resume la información visual relevante asociada con la severidad y características estructurales del daño vehicular.

La modalidad tabular estuvo compuesta por las siete variables seleccionadas durante el desarrollo del modelo TabularVelocityEstimator. Estas variables fueron procesadas mediante una red neuronal especializada encargada de proyectarlas a un espacio de representación compatible con los embeddings visuales.

La fusión multimodal se realizó mediante concatenación de ambas representaciones latentes, generando un vector conjunto utilizado como entrada para una red de regresión completamente conectada responsable de producir la estimación final de velocidad precolisión.

Durante el entrenamiento se empleó un tamaño de lote de 128 muestras, una tasa de aprendizaje inicial de $1\times10^{-4}$ y un máximo de 80 épocas. Adicionalmente, se incorporó un mecanismo de \textit{Early Stopping} con una paciencia de 10 épocas para prevenir sobreajuste y detener automáticamente el entrenamiento cuando el desempeño sobre el conjunto de validación dejara de mejorar.

La evaluación sobre el conjunto de prueba produjo un Error Absoluto Medio (MAE) de 4.45 km/h, constituyendo el mejor resultado obtenido durante el desarrollo del proyecto y superando significativamente el desempeño alcanzado por los enfoques unimodales basados exclusivamente en imágenes o variables tabulares.

La Tabla~\ref{tab:hybrid_best_config} resume los principales hiperparámetros y características de la configuración experimental seleccionada para HybridVelocityEstimator.

\begin{table}[H]
	\caption{Configuración experimental seleccionada para HybridVelocityEstimator}
	\label{tab:hybrid_best_config}
	\centering
	\footnotesize
	\setlength{\tabcolsep}{2pt}
	\renewcommand{\arraystretch}{1.05}
	
	\begin{tabularx}{\columnwidth}{|p{3.2cm}|X|}
		\hline
		\textbf{Parámetro} & \textbf{Valor} \\
		\hline
		
		\multicolumn{2}{|c|}{\textbf{Arquitectura}} \\
		\hline
		
		Encoder visual &
		ImageVelocityEstimator preentrenado \\
		\hline
		
		Dimensión de embedding visual &
		512 características \\
		\hline
		
		Modalidad tabular &
		7 variables seleccionadas mediante Pipeline tabular \\
		\hline
		
		Método de fusión &
		Concatenación de embeddings visuales y tabulares \\
		\hline
		
		Tarea &
		Regresión de velocidad precolisión \\
		\hline
		
		\multicolumn{2}{|c|}{\textbf{Entrenamiento}} \\
		\hline
		
		Batch Size &
		128 \\
		\hline
		
		Learning Rate &
		$1\times10^{-4}$ \\
		\hline
		
		Épocas máximas &
		80 \\
		\hline
		
		Early Stopping &
		Paciencia de 10 épocas \\
		\hline
		
		Precisión &
		bf16-mixed \\
		\hline
		
		Acelerador &
		GPU (CUDA) \\
		\hline
		
		Semilla aleatoria &
		42 \\
		\hline
		
		\multicolumn{2}{|c|}{\textbf{Datos}} \\
		\hline
		
		Número total de muestras &
		5\,202 \\
		\hline
		
		Entrenamiento &
		3\,329 muestras \\
		\hline
		
		Validación &
		832 muestras \\
		\hline
		
		Prueba &
		1\,041 muestras \\
		\hline
		
		Resolución de imágenes &
		$224 \times 224$ píxeles \\
		\hline
		
		\multicolumn{2}{|c|}{\textbf{Resultados}} \\
		\hline
		
		MAE de prueba &
		4.45 km/h \\
		\hline
		
		Tiempo total de ejecución &
		2\,219 s (36.98 min) \\
		\hline
		
	\end{tabularx}
	
\end{table}

Todos los experimentos correspondientes a HybridVelocityEstimator fueron ejecutados sobre una estación de trabajo portátil equipada con un procesador Intel Core i9-14900HX de 24 núcleos (32 hilos de ejecución), una unidad de procesamiento gráfico NVIDIA GeForce RTX 4060 Laptop GPU y 16 GB de memoria RAM. El entorno de ejecución estuvo basado en Debian GNU/Linux 12 (Bookworm), utilizando aceleración por GPU mediante CUDA y entrenamiento en precisión mixta (\textit{bf16-mixed}). Esta configuración permitió completar el proceso de entrenamiento y evaluación del modelo seleccionado en aproximadamente 2,219 segundos (36.98 minutos), proporcionando recursos computacionales suficientes para el entrenamiento eficiente de las arquitecturas multimodales propuestas.


\subsubsection{Diseño Modular y Reutilización del Encoder Visual}

Una de las decisiones arquitectónicas más relevantes implementadas en HybridVelocityEstimator consiste en reutilizar el modelo ImageVelocityEstimator como un encoder visual independiente encargado de generar las representaciones latentes utilizadas durante el proceso de fusión multimodal.

A diferencia de arquitecturas multimodales monolíticas entrenadas completamente de extremo a extremo, el enfoque propuesto desacopla la generación de características visuales del proceso de aprendizaje multimodal. Bajo esta estrategia, ImageVelocityEstimator es entrenado previamente hasta aprender un espacio latente especializado en la representación de daños vehiculares y posteriormente es reutilizado únicamente como extractor de características dentro de la arquitectura híbrida.

Esta decisión aporta múltiples ventajas desde la perspectiva computacional y de ingeniería de software. En primer lugar, permite reducir significativamente la cantidad de parámetros entrenables durante la etapa multimodal. Dado que ResNet50 contiene aproximadamente 25 millones de parámetros, congelar el encoder visual evita la necesidad de propagar gradientes a través de toda la red convolucional durante cada iteración de entrenamiento.

Como consecuencia, se reduce el consumo de memoria GPU, disminuyen los tiempos de entrenamiento y se facilita la experimentación con distintas configuraciones del bloque multimodal.

Adicionalmente, esta estrategia introduce una separación clara de responsabilidades entre componentes. Mientras ImageVelocityEstimator se especializa en aprender representaciones visuales del daño vehicular, HybridVelocityEstimator se enfoca exclusivamente en aprender la relación existente entre dichas representaciones, las variables tabulares y la velocidad objetivo.

Esta modularidad facilita significativamente la mantenibilidad y evolución futura del sistema.

\subsubsection{Escalabilidad y Evolución del Sistema}

El desacoplamiento arquitectónico implementado permite además aprovechar conjuntos de datos heterogéneos de manera independiente.

En escenarios reales, los conjuntos de datos multimodales completos, que contienen simultáneamente imágenes, variables tabulares y velocidades reconstruidas, suelen ser considerablemente más escasos que aquellos compuestos únicamente por imágenes etiquetadas. Gracias a la modularidad del diseño, ImageVelocityEstimator puede continuar entrenándose y mejorándose utilizando grandes volúmenes de datos visuales sin requerir la disponibilidad simultánea de variables tabulares asociadas.

Como consecuencia, cualquier mejora obtenida en la calidad de las representaciones visuales generadas por el encoder puede transferirse directamente al sistema multimodal sin necesidad de rediseñar la arquitectura híbrida. Embeddings más discriminativos permiten proporcionar información visual más rica al bloque de fusión, incrementando potencialmente la capacidad predictiva del modelo final.

Desde esta perspectiva, HybridVelocityEstimator constituye una arquitectura modular, escalable y sostenible, diseñada no sólo para maximizar el desempeño sobre el conjunto de datos disponible, sino también para facilitar futuras iteraciones de investigación y aprovechar nuevas fuentes de información conforme estas se encuentren disponibles.


\section{RESULTADOS}

\subsection{Resultados del Modelo Basado en Imágenes}

Esta sección presenta los resultados obtenidos por el modelo ImageVelocityEstimator, desarrollado para estimar la velocidad precolisión utilizando exclusivamente evidencia visual proveniente de imágenes de vehículos siniestrados. El desempeño del modelo se evaluó mediante el Error Absoluto Medio (MAE) sobre los conjuntos de validación y prueba, complementando el análisis cuantitativo con curvas de aprendizaje, diagramas de dispersión entre velocidades reales y estimadas, así como visualizaciones del espacio latente aprendido por la red neuronal. Estas representaciones permiten analizar tanto la capacidad predictiva del modelo como la calidad de las características visuales extraídas durante el proceso de entrenamiento.

El modelo alcanzó un MAE de validación de 11.504 km/h y un MAE de prueba de 12.534 km/h. Los resultados obtenidos demuestran que la información contenida en las deformaciones estructurales visibles después de una colisión resulta suficientemente informativa para que una red neuronal profunda aprenda relaciones significativas con la velocidad precolisión. 

Las curvas de aprendizaje muestran un proceso de convergencia estable tanto para los conjuntos de entrenamiento como de validación, sin evidencias significativas de sobreajuste durante las etapas finales del entrenamiento. La evolución consistente del Error Absoluto Medio a lo largo de las épocas sugiere que la arquitectura logró extraer características visuales relevantes y generalizables a partir de las imágenes disponibles. Las curvas de aprendizaje correspondientes se presentan en la Figura~\ref{fig:image_mae_curves}.

\begin{figure}[H]
	\centering
	\includegraphics[width=0.7\columnwidth]{Figures/mae_curves_ImageVelocityEstimator.png}
	\caption{Curvas de aprendizaje del Error Absoluto Medio (MAE) para el modelo ImageVelocityEstimator}
	\label{fig:image_mae_curves}
\end{figure}

El análisis del diagrama de dispersión entre velocidades predichas y velocidades reales permite caracterizar con mayor detalle el comportamiento del modelo a lo largo del rango dinámico de velocidades presente en el conjunto de datos. Para velocidades bajas y medias, las predicciones presentan una dispersión relativamente acotada alrededor de la línea de identidad, indicando que el modelo es capaz de capturar patrones visuales consistentes asociados con niveles moderados de daño estructural. Sin embargo, conforme la velocidad real aumenta, se observa una degradación progresiva en la precisión de las estimaciones.

Particularmente, a partir de aproximadamente 60 km/h comienza a manifestarse una tendencia sistemática de subestimación, donde eventos de alta severidad son proyectados hacia rangos de velocidad considerablemente inferiores. Este comportamiento resulta consistente con la distribución del conjunto de datos utilizado para entrenamiento, caracterizado por una representación significativamente menor de accidentes de alta energía en comparación con colisiones de baja y media severidad. Como consecuencia, el modelo dispone de una cantidad limitada de ejemplos para aprender adecuadamente las características visuales distintivas de impactos a velocidades elevadas. El diagrama de dispersión correspondiente se presenta en la Figura~\ref{fig:image_scatter}.

\begin{figure}[H]
	\centering
	\includegraphics[width=0.7\columnwidth]{Figures/predicted_vs_true_ImageVelocityEstimator.png}
	\caption{Diagrama de dispersión entre velocidades predichas y velocidades reales para el modelo ImageVelocityEstimator}
	\label{fig:image_scatter}
\end{figure}

Con el propósito de analizar la calidad de las representaciones internas aprendidas por la red neuronal, se realizó un estudio adicional sobre los embeddings visuales de 512 dimensiones generados por la arquitectura antes de la etapa final de regresión. Dichos embeddings constituyen la representación latente que el modelo construye sobre cada vehículo colisionado y permiten examinar si la red está organizando las muestras de acuerdo con patrones visuales relacionados con la variable objetivo.

Para facilitar su interpretación, los embeddings fueron proyectados a espacios bidimensionales mediante distintas técnicas de reducción de dimensionalidad. Los resultados obtenidos evidencian la aparición de agrupamientos y gradientes coherentes con la distribución de velocidades observadas en los datos. Las muestras correspondientes a velocidades similares tienden a concentrarse en regiones próximas del espacio latente, mientras que los accidentes asociados a mayores niveles de severidad se desplazan progresivamente hacia zonas diferenciadas de la representación aprendida.

Este comportamiento constituye evidencia directa de que la red neuronal logró capturar características visuales semánticamente significativas relacionadas con la magnitud del daño estructural y con la velocidad precolisión asociada a cada evento. Las visualizaciones del espacio latente se presentan en la Figura~\ref{fig:image_embeddings}.

\begin{figure}[H]
	\centering
	\includegraphics[width=0.8\columnwidth]{Figures/umap_true_speed_ImageVelocityEstimator.png}
	\caption{Visualización bidimensional de los embeddings generados por el modelo ImageVelocityEstimator}
	\label{fig:image_embeddings}
\end{figure}


\subsection{Resultados del Modelo Basado en Variables Tabulares}

Esta sección presenta los resultados obtenidos por el modelo de regresión basado en Red Neuronal Multicapa entrenado sobre las variables tabulares derivadas de la investigación forense de accidentes de tránsito. El desempeño del modelo se reporta mediante el Error Absoluto Medio (MAE), métrica seleccionada por su interpretabilidad directa en unidades físicas de velocidad (km/h) y su robustez frente a valores atípicos. Adicionalmente, se incluyen las curvas de aprendizaje del proceso de entrenamiento y un diagrama de dispersión entre velocidades predichas y velocidades reales para evaluar cualitativamente la capacidad de generalización del modelo.

\subsubsection{Desempeño del Modelo}

El modelo fue entrenado utilizando los 288 registros disponibles y el conjunto final de siete características seleccionadas durante el proceso de preprocesamiento. Sobre el conjunto de validación se obtuvo un MAE de 11.9 km/h, mientras que el conjunto de prueba registró un MAE de 7.3 km/h.

La diferencia observada entre ambas métricas puede atribuirse a la variabilidad estadística inherente a la partición de conjuntos de datos de tamaño moderado. No obstante, el desempeño alcanzado en el conjunto de prueba demuestra que las variables seleccionadas contienen información relevante para la estimación de la velocidad vehicular precolisión y permiten obtener predicciones con un nivel de error compatible con aplicaciones de análisis forense basadas exclusivamente en información tabular.

Las curvas de aprendizaje del modelo se presentan en la Figura~\ref{fig:mae_curves}. En ellas puede observarse la evolución del Error Absoluto Medio durante el proceso de entrenamiento tanto para el conjunto de entrenamiento como para el conjunto de validación. El comportamiento de ambas curvas evidencia una convergencia estable del proceso de optimización y confirma la efectividad de la estrategia de parada anticipada implementada durante el entrenamiento. No obstante, se observa una diferencia aproximada de 3 km/h de MAE entre ambas curvas al final del entrenamiento, siendo el error del conjunto de validación consistentemente superior al del conjunto de entrenamiento. Este comportamiento sugiere la presencia de un ligero sobreajuste (\textit{overfitting}), aunque su magnitud es moderada y no compromete la capacidad de generalización del modelo sobre datos no observados.

\begin{figure}[H]
\centering
\includegraphics[width=0.7\columnwidth]{Figures/mae_curves_TabularModelV3.png}
\caption{Curvas de aprendizaje del Error Absoluto Medio (MAE) para el modelo basado en variables tabulares}
\label{fig:mae_curves}
\end{figure}

Con el propósito de analizar la distribución de los errores a lo largo del rango de velocidades, se construyó un diagrama de dispersión entre los valores predichos por el modelo y las velocidades reales observadas. La Figura~\ref{fig:scatter_plot} muestra que las predicciones presentan la mayor precisión en el intervalo comprendido aproximadamente entre 15 y 60 km/h, donde los puntos se concentran alrededor de la línea de identidad y la dispersión permanece relativamente acotada.

En el rango de velocidades medias, comprendido aproximadamente entre 60 y 70 km/h, se observa un incremento moderado de la dispersión, aunque las predicciones mantienen una tendencia consistente respecto a los valores reales. Para velocidades superiores a 70 km/h aparece una tendencia sistemática de subestimación que se intensifica progresivamente conforme aumenta la severidad del evento. Este comportamiento es particularmente evidente en colisiones de muy alta energía, donde la escasa cantidad de observaciones disponibles limita la capacidad de generalización del modelo sobre regiones poco representadas del espacio de entrenamiento.

A pesar de esta limitación, la distribución global de los puntos evidencia una correspondencia razonable entre las velocidades predichas y observadas, lo que confirma la capacidad del modelo para capturar relaciones significativas entre las variables forenses seleccionadas y la velocidad asociada al accidente.

La Figura~\ref{fig:scatter_plot} presenta el diagrama de dispersión correspondiente.

\begin{figure}[H]
\centering
\includegraphics[width=0.7\columnwidth]{Figures/scatter_plot_TabularModelV3.png}
\caption{Diagrama de dispersión entre velocidades predichas y velocidades reales para el modelo basado en variables tabulares}
\label{fig:scatter_plot}
\end{figure}

En conjunto, los resultados obtenidos demuestran que es posible estimar la velocidad vehicular precolisión utilizando exclusivamente variables tabulares derivadas de investigaciones forenses, alcanzando un Error Absoluto Medio de 7.3 km/h sobre el conjunto de prueba. Estos resultados respaldan la hipótesis de que la información estructurada disponible en reconstrucciones de accidentes contiene suficiente capacidad descriptiva para modelar de manera efectiva la severidad cinemática del impacto sin requerir información visual adicional.


\subsection{Resultados del Modelo Híbrido}

Esta sección presenta los resultados obtenidos por la arquitectura multimodal \textit{HybridVelocityEstimator}, desarrollada para integrar simultáneamente información visual proveniente de imágenes postcolisión y variables tabulares derivadas de procesos de reconstrucción de accidentes. El objetivo principal de esta evaluación consiste en determinar si la combinación de ambas modalidades permite mejorar la precisión de las estimaciones de velocidad precolisión respecto a los enfoques unimodales desarrollados previamente.

El desempeño del modelo fue evaluado mediante el Error Absoluto Medio (MAE) sobre los conjuntos de validación y prueba, complementando el análisis con curvas de aprendizaje y diagramas de dispersión entre velocidades reales y estimadas. Los resultados obtenidos fueron posteriormente comparados con aquellos alcanzados por los modelos ImageVelocityEstimator y TabularVelocityEstimator con el propósito de cuantificar el impacto de la estrategia de fusión multimodal.

Los experimentos realizados mostraron una mejora consistente y significativa respecto a todos los enfoques desarrollados previamente. El modelo alcanzó un MAE de validación de 4.457 km/h y un MAE de prueba de 4.449 km/h, convirtiéndose en la arquitectura con mejor desempeño dentro de las alternativas evaluadas durante el proyecto.

Particularmente, se observó una reducción aproximada del 50\% en el error de predicción respecto al modelo basado exclusivamente en imágenes. Asimismo, el modelo híbrido logró disminuir entre un 35\% y un 40\% el error obtenido por el mejor modelo basado únicamente en variables tabulares. Estas mejoras constituyen evidencia cuantitativa de que ambas modalidades contienen información complementaria y que su integración permite construir representaciones más completas del fenómeno estudiado.

Las curvas de aprendizaje obtenidas durante el entrenamiento evidencian un proceso de convergencia estable y consistente. Tanto las métricas de entrenamiento como las de validación muestran una disminución progresiva del error a lo largo de las épocas, sin divergencias significativas entre ambas curvas. Este comportamiento sugiere que la arquitectura fue capaz de aprovechar eficazmente la información adicional proporcionada por cada modalidad, generando representaciones más robustas y generalizables que las obtenidas mediante los enfoques unimodales.

La Figura~\ref{fig:hybrid_mae_curves} presenta las curvas de aprendizaje correspondientes al modelo HybridVelocityEstimator.

\begin{figure}[H]
	\centering
	\includegraphics[width=0.7\columnwidth]{Figures/mae_curves_HybridModel.png}
	\caption{Curvas de aprendizaje del Error Absoluto Medio (MAE) para HybridVelocityEstimator.}
	\label{fig:hybrid_mae_curves}
\end{figure}

El análisis de las predicciones permite observar con mayor detalle las mejoras alcanzadas mediante la estrategia multimodal. La Figura~\ref{fig:hybrid_scatter} muestra el diagrama de dispersión entre velocidades reales y velocidades estimadas sobre el conjunto de prueba.

\begin{figure}[H]
	\centering
	\includegraphics[width=0.7\columnwidth]{Figures/predicted_vs_true_HybridModel.png}
	\caption{Diagrama de dispersión entre velocidades reales y velocidades estimadas para HybridVelocityEstimator.}
	\label{fig:hybrid_scatter}
\end{figure}


\section{DISCUSIÓN}

\subsection{Interpretación de Hallazgos}

Los resultados obtenidos a lo largo de los experimentos permiten extraer diversas observaciones relevantes sobre la capacidad de los modelos de inteligencia artificial para estimar la velocidad vehicular precolisión a partir de evidencia postimpacto. En términos generales, los tres enfoques desarrollados (basado en imágenes, basado en variables tabulares y multimodal) demostraron ser capaces de capturar información significativa relacionada con la severidad de las colisiones y utilizarla para aproximar la velocidad previa al impacto con niveles de error competitivos.

Uno de los hallazgos más relevantes corresponde al desempeño alcanzado por el modelo basado exclusivamente en imágenes. Los resultados obtenidos por ImageVelocityEstimator demostraron que las deformaciones estructurales visibles en fotografías postcolisión contienen información suficiente para que una red neuronal profunda aprenda representaciones asociadas con la velocidad de impacto. Este resultado respalda la hipótesis inicial de que los patrones de daño observables en la carrocería constituyen una manifestación indirecta de la energía involucrada durante el siniestro.

La evidencia más significativa de este comportamiento no proviene únicamente de las métricas de error obtenidas, sino también de la organización observada dentro del espacio latente generado por la red neuronal. Las visualizaciones de los embeddings mostraron agrupamientos coherentes con diferentes rangos de velocidad, indicando que el modelo logró construir representaciones semánticamente significativas del daño vehicular. Este hallazgo sugiere que las redes neuronales profundas son capaces de identificar características visuales relevantes para la estimación de velocidad incluso en ausencia de variables estructuradas complementarias.

Por otra parte, los resultados obtenidos mediante los modelos basados en variables tabulares demostraron que la información derivada de investigaciones forenses y procesos de reconstrucción de accidentes posee una elevada capacidad predictiva para la estimación del delta-V precolisión. Particularmente, los experimentos realizados evidenciaron que la selección adecuada de características tiene un impacto considerable sobre el desempeño final del modelo. La mejora observada en MLP-P3 respecto a las configuraciones anteriores indica que la eliminación de variables redundantes o de baja relevancia permite construir representaciones más robustas y mejorar la capacidad de generalización sobre datos no observados.

Asimismo, la convergencia observada entre los resultados obtenidos por MLP-P3 y XGBoost constituye un hallazgo relevante desde una perspectiva metodológica. El hecho de que arquitecturas pertenecientes a paradigmas de aprendizaje distintos alcancen niveles de desempeño similares sugiere que el subconjunto de variables seleccionado captura efectivamente la información más relevante para la estimación de velocidad. Esta consistencia fortalece la validez del proceso de selección de características implementado y sugiere la existencia de un límite práctico de desempeño asociado exclusivamente a la información tabular disponible.

Sin embargo, el hallazgo más importante del estudio corresponde al desempeño alcanzado por la arquitectura multimodal HybridVelocityEstimator. La mejora significativa obtenida respecto a todos los enfoques unimodales proporciona evidencia sólida de que las modalidades visual y tabular contienen información complementaria sobre el fenómeno estudiado. Mientras que las imágenes permiten capturar información relacionada con la geometría del daño, la distribución de deformaciones y la severidad observable de la colisión, las variables tabulares aportan contexto técnico adicional asociado con las características del accidente y los vehículos involucrados.

La reducción del error obtenida mediante la fusión de ambas modalidades sugiere que ninguna de ellas resulta suficiente por sí sola para describir completamente la complejidad física de una colisión vehicular. En cambio, la integración conjunta permite construir representaciones más completas del evento, disminuyendo la incertidumbre presente en cada modalidad individual y mejorando la capacidad predictiva del modelo final.

Adicionalmente, los resultados obtenidos validan la estrategia de reutilización del encoder visual dentro de la arquitectura multimodal. Los embeddings generados por ImageVelocityEstimator demostraron contener información discriminativa valiosa sobre la severidad de los daños, funcionando como una representación compacta y altamente informativa que pudo integrarse exitosamente con variables estructuradas. Este resultado respalda la viabilidad del diseño modular propuesto y abre la posibilidad de continuar mejorando el encoder visual de manera independiente en futuras iteraciones del sistema.

En conjunto, los hallazgos obtenidos sugieren que la estimación automática de velocidad precolisión constituye un problema inherentemente multimodal. Tanto la evidencia visual como la información derivada de procesos de reconstrucción contienen señales relevantes para inferir la velocidad de impacto, y la combinación de ambas fuentes permite alcanzar niveles de precisión significativamente superiores a los obtenidos mediante enfoques unimodales.

\subsection{Limitaciones}

A pesar de los resultados obtenidos, el presente estudio presenta diversas limitaciones que deben considerarse al interpretar los hallazgos y al evaluar el potencial de generalización de los modelos desarrollados.

La principal limitación identificada corresponde al tamaño y distribución del conjunto de datos disponible. Aunque el dataset construido permitió desarrollar y evaluar las arquitecturas propuestas, la cantidad total de muestras continúa siendo relativamente reducida para el entrenamiento de modelos de aprendizaje profundo. Esta situación resulta particularmente evidente en los rangos superiores de velocidad, donde la cantidad de observaciones disponibles disminuye considerablemente.

El desbalance presente en la distribución de velocidades constituye una segunda limitación importante. La mayoría de los casos analizados corresponden a colisiones de baja y media severidad, mientras que los accidentes de alta energía se encuentran significativamente subrepresentados. Como consecuencia, todos los modelos evaluados mostraron una tendencia consistente a incrementar sus errores de predicción conforme aumenta la velocidad real de los eventos analizados. Este comportamiento fue especialmente evidente para velocidades superiores a aproximadamente 65 km/h y representa el principal factor limitante para la generalización en escenarios de alta severidad.

Otra limitación relevante está asociada con la naturaleza de los datos utilizados como referencia. Las velocidades empleadas como variable objetivo provienen de procesos de reconstrucción forense y simulación de accidentes, los cuales, aunque constituyen la mejor aproximación disponible al valor real, siguen estando sujetos a incertidumbre inherente a los procedimientos de estimación. En consecuencia, los modelos fueron entrenados para aproximar reconstrucciones especializadas de la velocidad precolisión y no mediciones directas obtenidas durante el evento real.

Adicionalmente, el conjunto de datos utilizado proviene principalmente de casos documentados dentro del programa CIREN, lo que podría introducir sesgos relacionados con los criterios de selección de casos, las condiciones particulares de recopilación de evidencia y la distribución geográfica de los accidentes incluidos en la base de datos. Por esta razón, la capacidad de generalización de los modelos sobre poblaciones vehiculares diferentes o sobre contextos de operación distintos requiere ser validada mediante conjuntos de datos adicionales.

Finalmente, el modelo basado exclusivamente en imágenes presenta limitaciones inherentes a la naturaleza de la información visual disponible. Aunque las deformaciones observables contienen información relevante sobre la severidad del impacto, existen múltiples factores físicos que influyen en la dinámica de una colisión y que no pueden inferirse completamente a partir de fotografías postimpacto aisladas. Esta observación constituye precisamente una de las motivaciones que condujo al desarrollo del enfoque multimodal propuesto.

En consecuencia, los resultados presentados deben interpretarse como una validación inicial de la viabilidad del enfoque planteado y como una base para investigaciones futuras orientadas a incrementar la cantidad, diversidad y calidad de los datos disponibles.


\section{CONCLUSIONES}

\subsection{Resumen final}

El presente trabajo investigó la viabilidad de estimar automáticamente la velocidad vehicular precolisión mediante técnicas de inteligencia artificial aplicadas a evidencia obtenida durante procesos de reconstrucción de accidentes. Para ello se desarrollaron y evaluaron tres enfoques complementarios: un modelo basado exclusivamente en imágenes postcolisión, un conjunto de modelos basados en variables tabulares derivadas de investigaciones forenses y una arquitectura multimodal capaz de integrar simultáneamente ambas fuentes de información.

Los resultados obtenidos demostraron que las deformaciones estructurales visibles en vehículos siniestrados contienen información significativa relacionada con la velocidad de impacto y que dicha información puede ser capturada mediante técnicas modernas de visión computacional. Asimismo, se comprobó que las variables tabulares derivadas de procesos de reconstrucción poseen una elevada capacidad predictiva y permiten estimar la velocidad precolisión con niveles de error competitivos.

El hallazgo principal del estudio corresponde al desempeño alcanzado por la arquitectura multimodal HybridVelocityEstimator. La integración de embeddings visuales con variables estructuradas permitió obtener mejoras sustanciales respecto a todos los enfoques unimodales evaluados, validando la hipótesis de que ambas modalidades contienen información complementaria sobre el fenómeno físico subyacente.

Los resultados obtenidos evidencian que la estimación automática de velocidad precolisión constituye un problema inherentemente multimodal y que la combinación de información visual y contextual permite construir representaciones más completas y precisas de los accidentes vehiculares. En este sentido, el enfoque propuesto demuestra el potencial de las técnicas de inteligencia artificial como herramientas de apoyo para procesos de análisis forense y reconstrucción de accidentes.

Adicionalmente, el diseño modular implementado permitió demostrar la utilidad de reutilizar representaciones visuales aprendidas mediante transferencia de conocimiento, favoreciendo la escalabilidad, mantenibilidad y evolución futura del sistema. Este enfoque abre nuevas posibilidades para el desarrollo de soluciones multimodales capaces de incorporar información proveniente de múltiples fuentes dentro de entornos de investigación forense automatizada.

\subsection{Trabajo futuro}

Los resultados obtenidos durante esta investigación abren múltiples líneas de trabajo orientadas a mejorar tanto el desempeño predictivo como la capacidad de generalización de las arquitecturas desarrolladas.

La dirección más inmediata consiste en ampliar el conjunto de datos disponible, particularmente mediante la incorporación de casos correspondientes a colisiones de alta energía. Una representación más equilibrada de todo el espectro de velocidades permitiría reducir las limitaciones asociadas al desbalance actualmente observado y mejorar el desempeño de los modelos sobre eventos de alta severidad.

De manera complementaria, futuras investigaciones podrían explorar estrategias avanzadas de aprendizaje multimodal, incluyendo mecanismos de atención, técnicas de fusión intermedia y arquitecturas basadas en transformadores multimodales. Estos enfoques podrían permitir una integración más profunda entre las modalidades visual y tabular, favoreciendo la captura de relaciones complejas entre ambas fuentes de información.

Otra línea prometedora consiste en continuar la evolución independiente del encoder visual mediante el entrenamiento sobre conjuntos de imágenes significativamente más grandes y diversos. Dado que la arquitectura propuesta desacopla la generación de embeddings visuales del proceso de aprendizaje multimodal, cualquier mejora obtenida en la calidad de estas representaciones podría transferirse directamente al sistema híbrido sin necesidad de rediseñar su estructura general.

Asimismo, resultaría valioso incorporar nuevas fuentes de información al proceso de estimación, tales como datos de telemetría vehicular, información proveniente de registradores de eventos, características geométricas detalladas de los vehículos involucrados o reconstrucciones tridimensionales obtenidas mediante técnicas fotogramétricas.

Finalmente, futuras investigaciones deberían evaluar la capacidad de generalización de los modelos sobre conjuntos de datos independientes, procedentes de diferentes regiones geográficas, tipos de vehículos y contextos de operación. Este proceso permitiría validar la robustez del enfoque propuesto y avanzar hacia el desarrollo de sistemas capaces de asistir de manera confiable en escenarios reales de reconstrucción y análisis forense de accidentes vehiculares.



% Referencias o fuentes bibliograficas
\insertbibliography{References}

\end{document}
